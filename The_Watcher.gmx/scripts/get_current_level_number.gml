//Used to get the current level number
//level names must follow as level_##
//where # is any number
//The name can't have any other positions with a number
//or this won't work
return real(string_digits(room_get_name(room)))
