/* Creates a random string of characters given a range of lengths
 * @param arg0 - length of the string
 */

str = ''
for(var i=0; i<argument0; i++){
    str += chr(irandom_range(ord('A'), ord('Z')))
}

return str
